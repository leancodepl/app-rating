name: app_rating
description: A new Flutter package project.
version: 0.0.1

library:
  - lib/rating.dart

environment:
  sdk: ">=3.1.0 <4.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  bloc_presentation: ^1.0.0
  cqrs: ^10.0.1
  google_fonts: ^6.1.0
  flutter_bloc: ^8.1.3
  in_app_review: ^2.0.8
  leancode_hooks: ^0.0.6
  leancode_contracts: ^0.8.0
  freezed_annotation: ^2.4.1
  intl: ^0.18.1
  flutter_localizations:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^2.4.6
  flutter_lints: ^2.0.0
  freezed: ^2.4.5
  leancode_contracts_generator: ^0.12.0

flutter:
  assets:
    - packages/app_rating/assets/star-selected.png
    - packages/app_rating/assets/star-unselected.png
  generate: true
